/* ============================================================
   case-study.js — page-specific scripts for case study pages
   main.js handles: scroll reveal, active nav, progress bar, side nav
   This file initialises those behaviours that depend on the DOM
   being ready, and wires up any case-study-only interactions.
   ============================================================ */

document.addEventListener('DOMContentLoaded', () => {
  /* main.js already runs synchronously and attaches observers,
     but IntersectionObserver fires async — nothing extra needed.
     This file is a hook for future case-study-specific JS. */
});
